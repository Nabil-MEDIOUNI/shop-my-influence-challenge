export type InsightType = {
  id: number;
  title: string;
  description: string;
  picture: string;
  background: string;
};

export type SaleType = {
  type: string;
  title: string;
  price: string;
  picture: string;
  color: string;
};
